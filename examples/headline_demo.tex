\documentclass{beamer}
\usecolortheme{spruce}
\setbeamercolor{frametitle}{bg=}  % Otherwise frametitle overlaps headline


\usepackage{heuristica} % Just for cosmetic purposes

\usepackage{calc}
% -- Since .sty and .txt are not in same directory, we have to give the
% -- templatefile argument and use ../gwbar
\usepackage[
    templatefile="../code/generic_template_no_noise_whitened.txt",
    backgroundsignal="../code/generic_template_w_noise.txt",
    declaretitlebox=false,
    signalupshift=-24pt,
]{../gwbar}
% -- Note: with this signalupshift, it sits nicely between frametitle and
% -- framesubtitle. But on frames with no subtitle, there is an annoying
% -- bit of overlapping frametitlebox. This is solved in the advanced
% -- version of this file.


% -- Note: the following two are equivalent
% \setbeamertemplate{headline}[gwbar]
\addtobeamertemplate{frametitle}{}{\gwbar}


\usepackage{tikz}

\begin{document}

% -- Before begin of document, frametitle.fg is not known
\tikzset{
    gwbar@linelayerone/.style={
        frametitle.fg,
        smooth,
        line width=0.7pt,
	},
}


\begin{frame}[t]{Frame Title}
This is the first slide
\end{frame}

\begin{frame}[t]{Frame Title}{Frame Subtitle}
Slide number 2
\end{frame}

\begin{frame}[t]{Frame Title}
Another slide
\end{frame}

\begin{frame}[t]{Frame Title}{Frame Subtitle}
This is the last slide
\end{frame}

\end{document}
